# .github/workflows/project-a.yml
name: Build Project A

on:
  push:
    branches: [main]

jobs:
  call-maven:
    uses: Mddh-23/CI-Pipeline-Template/.github/workflows/reusable-maven.yml
    with:
      java-version: '17'
      goals: 'clean install'
